temp_atual = float(input("Digite a temperatura atual: "))
temp_perm = 25

if temp_atual > temp_perm:
    print("Alerta temperatura está acima do permitido.")

else:
    print("Temperatura dentro do permitido.")